use crate::types::{Commodity, Station, StationMarket};

/// Solves an instance of the bounded knapsack problem using linear programming
pub fn solve_knapsack(
    station1: StationMarket,
    station2: StationMarket,
    capacity: u32,
    capital: u64,
) {
}
